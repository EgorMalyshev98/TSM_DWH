[project]
name = "tsm-dwh"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "loguru==0.7.3",
    "pandas==2.2.3",
    "python-dotenv==1.0.1",
    "dv-gen",
    "dbt-core>=1.11.7",
    "dbt-postgres>=1.10.0",
]

[tool.uv.sources]
dv-gen = { path = "dv_gen", editable = true }
